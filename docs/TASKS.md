# pulse-check — Tasks

Wave-by-wave forward plan. Each wave is a flat checklist of high-level deliverables. Sub-bite breakdowns + per-bite breadcrumbs live in [`SESSION_LOG.md`](SESSION_LOG.md) (the historical record).

**Status:** Wave 2 ✅ substantively ships. Wave 5 Stage A + B complete: all 53 in-corpus products tagged + aggregated + briefed under `run_wave5_v1` (6/59 products skipped — zero qualifying mentions). Brief schema bumped to `a1_brief_v2` with per-claim `header` field rendered as card-per-claim (DESIGN_SYSTEM §5.2 patched session 33). UI shipped: 3-card home page (Standalone voice · Head-to-head · Cross-product heatmap), now with **purple-outlined button CTAs** instead of text-hyperlinks (session 35); separate Sources accordion + collapsible "Under the hood" pipeline explainer; Standalone empty-state Company/Product picker (no default product); Head-to-head Pair page (A1-based, 3-count tiles + single combined table sorted by combined mention count, leader-side accent ring, Pair idle-state explainer card added session 35); Cross-product heatmap polished (multi-select popovers · Company column with brand banners · sortable headers on `Company` + 11 aspects with asc/desc/clear cycle · color-scale legend above the grid · row-hover dim affordance · cascading Company → Size → Product filters · screen-size regex now recovers `16x` / `16s` / `Z13` / `X16` and size-less products). All three data pages carry a **soft-lavender `What are all these numbers?` glossary trigger** (session 35; 6 entries × 5 sections); Standalone additionally carries an **`Export brief` trigger** (session 35) opening a centered overlay with a single-page A4 one-pager and Save-as-PDF / Download-HTML actions. ASUS / ASUS ROG merged into a single `ASUS` brand at both the YAML config and DB `products`-table layer (session 35). `EvidenceDrawer` verified-only filter hidden (corpus has zero verified-purchase signal until BestBuy/Amazon ingestion lands). DESIGN_SYSTEM §6.1 / §6.2 / §6.3 / §6.4 + §2.11 / §2.12 patched session 35 to match shipped reality; ARCHITECTURE §3.2 annotates A2 schema tables as parked. Wave 3 A2 path **parked** (Reddit corpus structurally thin on resolved deliberation; tables retained empty in schema for future revival). Public deployment live via Tailscale Funnel: uvicorn :8765 → `/pulse-check`. README is the operator runbook. **Session 36 ships:** quarterly refresh cadence locked (supersedes session-31 monthly; README updated across 3 spots with `$60–100/year` cost figures; About header gained underlined `Refreshed quarterly.` line); MSI attribution regex refined with `a?` prefix on size digits across 11 MSI products × 2 patterns = 22 line edits in `configs/product_set_gaming_laptops_2026.yaml` (AMD-variant `Crosshair A17` / `Katana A15` / `Cyborg A14-A17` / `Vector A16-A18` now match alongside their Intel counterparts), cascade fired (+66 SECONDARY attributions · +85 aspect_tag rows · +11 aggregates 489→500 · +9 briefs at `a1_brief_v2` 53→62; `msi_crosshair_17` zero-to-briefable; `msi_cyborg_15` newly grounded in 9 cite IDs); Notebookcheck `search_notebookcheck()` gained `manufacturer` kwarg + `_MANUFACTURER_IDS` brand-ID dict (7 brands recon'd live) + 2 unit tests (curation pass deferred until all session-36 polish bites land); operator-side Ollama + Qwen + Playwright prereqs confirmed installed. **Session 37 ships:** bite #3 F1 closure landed via metric change rather than prompt iteration — diagnosis pass refuted the prior "Sonnet labeler over-labels NotebookCheck Verdict prose" hypothesis (intensity-only wobble was 83/90 of disagreements, not aspect-detection error); `compute_micro_f1_loose` added to `pulse_check/eval/eval_runner.py` with ±1 intensity-bucket tolerance + greedy one-to-one matching within `(aspect, polarity)` groups; `EvalReport` gained `micro_loose` field and `passed` now gates on loose; `scripts/run_eval.py` defaults `--exclude-content-types` to `deal` (mirrors `run_stage_b.py` production gate, closes session-19 filter-mirror bug); both strict and loose F1 printed in CLI summary and persisted in JSON; re-eval result: strict F1=0.6000 (unchanged) / **loose F1=0.7783** (TP=358 FP=74 FN=130; precision 0.83, recall 0.73); THRESHOLD=0.80 kept (FAIL banner expected, operator-accepted as Wave 2 closure number). **Session 38 ships:** bite #5 link-rot / tombstoning — code shipped, live verification deferred. New `pulse_check/verifiers/link_verifier.py` (`verify_url` + `verify_all_pending`; HEAD-first with GET-stream 405 fallback; conservative rule writes `tombstoned_at` only on 404 / 410 / DNS / refused, leaves 403 / 5xx / timeout alive); new `scripts/verify_mention_links.py` CLI; aggregation filter (`Mention.tombstoned_at IS NULL` at mention-pool fetch + tag-drop for tombstoned mentions, so `_compute_bucket` arithmetic stays clean) — forward-only on new runs, existing `run_wave5_v1` aggregates frozen by baked-in `mention_ids`; `MentionView` gained `tombstoned_at` + `tombstone_reason`; `VerbatimCard.tsx` renders `<Chip tone="tombstone">link dead</Chip>` in tag row 2 (session-13 lock lifted with operator approval); 15 verifier unit tests + 1 integration test (`test_aggregate_excludes_tombstoned_mentions`); ARCH §9 REVERSED from "remain contributors" to "excluded from new aggregations" + verifier reference + conservative rule documented; DESIGN_SYSTEM §4.1 + §4.8 lifted session-13 tombstone-marker lock; PRD §12 exclude-vs-include now answered (retention window still open); TESTING §4 bumped to 3 integration tests. **Live HEAD batch NOT run** — dry-run on 5,061 URLs at workers=16 returned `{http_429: 4585, http_403: 33, ok: 443}` because the corpus is 96.8% reddit and anonymous concurrent HEAD requests get rate-limited; conservative rule meant a live run would tombstone 0 (same outcome, no information gain). Bite-#5 follow-on (reddit-domain throttling or authenticated reddit API) queued for session 39. **Session 39 ships:** three polish bites in one sitting — bite #6 quarterly-cadence scheduler skeleton (new `pulse_check/scheduling/state.py` + `scripts/refresh.py` dry-run-default orchestrator + `scripts/refresh_quarterly.{ps1,xml}` Windows Task Scheduler artifacts + 24 unit tests; state-on-success-only discipline so a crash mid-chain leaves `last_refresh_at` unchanged), bite #8 design-system audit reconciliation (doc-only, 4 edits: §2.7 + §3.2 "aspirational" annotations on spacing + typography tokens; §4.1 Chip tone table extended with `primary` + `secondary` rows; new §6.5 for the dev-only Showcase route), bite #9 markdown brief export (new `frontend/src/lib/exportMarkdown.ts` pure function + `BriefExportButton.tsx` third toolbar action; citation numbering pinned to agree with the visual sheet via shared `Map<mention_id, n>` rebuild from the same `citedIds` order). Bite-#5 follow-on (reddit-domain throttling) now the only remaining polish-queue item; exit forks unblock after it lands. **Session 40 ships:** bite-#5 follow-on reddit-domain throttle landed — per-domain `threading.Lock` + 1.5 s sleep gate in `verify_all_pending` (new `_is_reddit_host` helper covering `reddit.com` + `www.` / `old.` / `m.` subdomains; CLI `--reddit-throttle-sec` flag default 1.5; +3 verifier unit tests covering serialization-at-max-concurrent-1, sleep-only-for-reddit, subdomain-variant matching); smoke test on 50 URLs returned 50/50 ok at workers=4 vs session-38's 4585/5061 http_429 at workers=16; live HEAD batch on the full 5061 corpus completed in 2h 07min (14:13:19 → 16:20:44) returning `{ok: 5028, http_403: 33}` and 0 tombstones — throttle eliminated reddit's 90.6% 429 rate, the 33 × 403s are retailer bot-blocks the conservative rule deliberately leaves alive. WelcomeModal overlay added on home-fresh-visits (`frontend/src/components/WelcomeModal.tsx` + `App.tsx` location-aware mount via `useState(() => location.pathname === "/")` lazy initializer so in-app navigation does not re-trigger but F5 / new tab does; 3-section grid problem / what / who with `AlertCircle` / `Sparkles` / `Users` lucide icons + inline SVG diagrams [scattered sources → faded "?", funnel mentions → 11 aspect bars → brief card, 4-persona row]; `bg-surface-alt/40` "How each function uses it" panel with PM / Mkt / Eng / Sales pill rows + one-line use cases; X / backdrop / Escape / Got-it dismissal all routed to the same `onClose`). First frontend test framework lands: Vitest 4.1.7 + `@testing-library/react` 16.3 + `@testing-library/jest-dom` 6.9 + jsdom 29.1; `vite.config.ts` extended with `test` block via `defineConfig` from `vitest/config` for type integration; `src/test-setup.ts` registers matchers + auto-cleanup; 13 tests on first pass (5 on `briefToMarkdown` + 8 on `WelcomeModal`); npm scripts `test` (watch) / `test:run` (one-shot). DESIGN_SYSTEM §6.6 WelcomeModal entry + TESTING §7 frontend-tests section rewritten to match shipped state.
**Session 41 ships:** operator-requested source-coverage + truncation audit + executive-brief paragraph. (1) Source-coverage audit confirmed all enabled sources are pulling — Reddit 4,898 mentions (96.8%) · YouTube 119 transcript chunks across 12 videos (2.35%) · Articles 44 (33 Notebookcheck curated + 11 RSS via PCMag / Tom's / TechRadar; Engadget / Verge / Ars 0 — title-filter narrow) · BestBuy / Amazon 0 by config. YouTube is not silent — pull is narrow because the RSS title filter is strict and only 7 channels are seeded. (2) Truncation audit found one real cap: `pulse_check/tagging/content_type_classifier.py` was clipping mentions to 3000 chars before `review / deal / other` triage; bumped to 25000 + bumped `PROMPT_VERSION` v1 → v2; re-tagged the full 5061-mention corpus (5061 inserted, 0 parse failures, ~$5 Haiku, ~98 min wall). Diagnostic on flips: 6 rescue (v1=deal → v2=other, all reddit_comment) + 4 contrary (v1=other → v2=deal). **Zero articles flipped** — the 9 deal-tagged articles stayed deal-tagged even with full text; LLM was reading them correctly from the prefix. Cap fix is still correct (Stage A no longer silently truncates) but rescue effect is modest. (3) Re-ran the aspect tagger to pick up the 6 rescued + any empty-result pairs whose idempotency-by-(mention,product) lets them re-classify; +671 aspect_tags inserted (mostly from previously-empty Qwen results that now surface aspects), aggregates upserted 500 → 502 rows, total `aspect_tags` 4898 → 5569. (4) **A1 brief writer bumped `a1_brief_v2` → `a1_brief_v3`** adding a top-level `BriefSummary` field (text + cited_mention_ids) in `pulse_check/synthesis/contracts.py`. Single Sonnet call now produces summary + claims together; new `_parse_summary` helper filters cited IDs against the input pool in Python (defense-in-depth so Sonnet can't fabricate). `citation_validator` walks summary cites in fabricated/out-of-pool checks. Frontend renders summary paragraph on three surfaces: `BriefPanel.tsx` (on-page, italic left-border-accent aside between header and "What's working" block), `BriefExportButton.tsx` (A4 sheet, between Aspect snapshot and Top strengths; new `summaryBlock` style with purple left-border + lavender bg + italic body + trailing `[n, m]` cite brackets that inherit the citation footer numbering), `frontend/src/lib/exportMarkdown.ts` (markdown export, new ## Summary section between snapshot table and Top strengths). Cite numbering in the export was extended in `extractTopClaims` to put summary cites FIRST so they get `[1]`, `[2]`, `[3]` (matches reading order). The field was briefly named `vibe_summary` mid-session and renamed to `summary` later in session 41 per operator-preferred register; parser accepts the legacy key from cached Sonnet responses, and a one-shot DB rewrite normalized the in-flight v3 briefs to the canonical key. +9 backend tests (5 brief_writer covering parsed-when-present / absent-yields-None / cited-ids-filtered-to-pool / 600-char-truncation / malformed-yields-None / legacy-key-fallback; 3 citation_validator covering summary-fabricated-id / summary-out-of-pool / summary-None-no-effect; +1 mock-client extension) + 4 frontend vitest (BriefPanel renders summary / hidden when null / cite-chip click invokes onCite with mention IDs / strengths-weaknesses unchanged when summary null). Full 62-brief regen under v3 ran (~$12 Sonnet, ~20 min wall). Smoke test on `alienware_18_area_51` validated paragraph quality before the full regen (62-word 2-sentence paragraph weaving topic texture + convergence-vs-polarization framing with 3 citations).
**Session 42 ships:** Pair brief (single-contrast-paragraph A2 artifact, designed session 41 and deferred to 42). New `PairBriefContrast` + `PairBriefNarrative` in `pulse_check/synthesis/contracts.py` (800-char ceiling vs A1's 600 — pair contrast enumerates aspects on both sides; cap bump approved after first batch found 2/10 briefs truncated at 600). New `pulse_check/synthesis/pair_brief_writer.py` mirrors `brief_writer.py` shape: pure-Python routing (`_compute_pair_entries` lazy-joins both products' `aggregates_aspect_sku` rows, drops aspects under 3 mentions on both sides, sorts by `abs(delta)` desc), Sonnet writes brief_title + 50–80-word 2-sentence even-handed contrast paragraph forbidding counts/percentages/winner-verdicts; cite-pool filter against union of both sides' verbatim pools; placeholder branch when no aspect crosses threshold. `citation_validator.validate_pair_citations` is a strict subset of A1's check (fabricated + out-of-context + zero-cite, no drift/empty-claims walks). `orchestrator.synthesize_pair` persists `Brief` row with `scope_type=ASPECT_2_PAIR` + `scope_id=f"{primary}_vs_{competitor}"` — NO schema migration (existing polymorphic `briefs` table accommodates). New `scripts/synthesize_pair.py` CLI (`--pair-plan` + `--pair-id` single / `--limit` smoke / batch); commit-every-1 mid-batch durability. API: `_latest_pair_brief_id` helper + `latest_pair_brief_id: int | None` field on `PairResponse`; existing `/api/brief/{brief_id}` already passes pair narrative through as-is. Frontend: new `PairBriefPanel.tsx` (mirror of `BriefPanel.SummaryBlock` styling — accent left-border italic surface-alt aside), `types.ts` adds `PairBriefContrast` + `PairBriefNarrative` + `latest_pair_brief_id` on `PairResponse`; Pair page fetches brief via existing `api.brief(briefId)` once `latest_pair_brief_id` resolves; cite-chip clicks open the existing EvidenceDrawer under a "Contrast" label since pair cites are pooled across both products (no per-product attribution). Smoke ran 2 pairs against `run_wave5_v1` validating prompt — clean output, no fabrication, ~$0.04. Full batch on all 10 pairs in `configs/pair_plan_alienware_vs_all.yaml` ran in ~80 s (~$0.20 Sonnet); 0/10 flagged by validator. Re-run after the 600→800 cap bump cache-hit on all 10 — the previously-truncated 2/10 now land with full final sentences. ARCHITECTURE §6.3 added "Pair brief layout" subsection documenting shape + rules + routing + placeholder + cost. +21 backend tests (14 pair_brief_writer covering happy-path / placeholder-when-under-threshold / placeholder-no-aggregates / fabricated-cite-filtered / cross-side-cite-allowed / leader-primary / leader-comparator / leader-tie / aspect-below-threshold-dropped / one-sided-aspect-kept / sort-by-abs-delta / unknown-prompt-version / malformed-contrast / strict-prompt-preamble; 5 validate_pair_citations covering clean / fabricated / out-of-context / placeholder-with-empty-cites / non-placeholder-with-zero-cites) + 2 api test_app pair (advertise-latest-pair-brief-id / null-when-none) + 1 integration `test_synthesize_pair_persists_brief_with_contrast`. +4 frontend vitest on PairBriefPanel (renders title+contrast / invokes-onCite-with-mention-ids / hidden-cite-chip-on-empty / model+prompt-version-in-header). 22 pair-brief rows persisted under run_wave5_v1 (brief_ids 173–174 smoke + 175–184 first batch superseded by 185–194 post-cap-bump; `_latest_pair_brief_id` returns 185–194 per pair).
**Session 43 ships:** (1) Pair brief saturation — distinct pair scope_ids 10 → **200** under `run_wave5_v1` across 3 staged batches: `configs/pair_plan_wave5_staged.yaml` (36 pairs) → `configs/pair_plan_alienware_full_v1.yaml` (200 pairs, crashed mid-batch at 102/200 on credit exhaustion) → `configs/pair_plan_alienware_full_v1_remaining.yaml` (81 cleanup pairs). Coverage now: 4 Alienware primaries (16 Aurora · 16x Aurora · 16 Area-51 · 18 Area-51) × 50 non-Alienware competitors with aggregates (5 universe products skipped — zero aggregate rows under `run_wave5_v1`). 240 pair brief rows persisted (extra above 200 = cap-bump re-runs from session 42 + post-truncation re-syntheses); `_latest_pair_brief_id` returns the freshest. 0 validator flags across all 200. (2) **Comparative export** — new `frontend/src/components/PairBriefExportButton.tsx` + `frontend/src/lib/pairExportMarkdown.ts` mirror standalone `BriefExportButton` shape on the Pair page (3 routes: PDF via `window.print` + `body.printing`, self-contained HTML Blob, GFM Markdown). A4 sheet diverges where the data does: 3 stat tiles (primary leads / ties / competitor leads instead of 4 aspect-tone counts) · two per-product 11-aspect chip rows · accent left-border italic contrast block · "Where {primary} leads" / "Where {competitor} leads" leader bullets (top 3 per side by `abs(delta) > 0.10`, each citing up to 2 mention_ids from the leading side's pool) · pooled citations footer. `_PAIR_LEAD_THRESHOLD = 0.10` shared with `/api/pair` scorecard so the prose tracks the visual. Wired into Pair page header right of `GlossaryButton` conditional on both `pairState.kind === "ready"` AND `briefState.kind === "ready"`. (3) **Cost remediation** — `VERBATIM_TEXT_CAP_CHARS = 800` truncation in `pair_brief_writer._verbs_block` + `brief_writer._truncate_verbatim` (raw mention `raw_text` avg 977 chars, max 72,945; pre-truncation per-pair input ballooned to ~165K Sonnet input tokens / ~$0.50, vs documented ARCHITECTURE figure of `$0.02/pair` that was wrong by 25×). Post-truncation measured ~$0.030/pair across the 81-pair cleanup batch (final spend $2.40 vs $2.79 estimate; in_tokens avg 8,618, out_tokens avg 253). Truncation invalidates pre-session-43 LlmCache rows by changing the input_hash but persisted `briefs` rows are unaffected. (4) **Per-call cost logging** — `pulse_check/synthesis/anthropic_client._call` now logs `anthropic call model=... in_tokens=... out_tokens=... est_cost=$x.xxxx` at INFO after every `messages.create`, backed by a `_PRICING_PER_MTOKEN` table covering Sonnet 4.6 / Haiku 4.5 / Opus 4.7. Future batches surface spend in-flight instead of post-hoc on the billing dashboard. (5) **Docs** — ARCHITECTURE §6.3 cost paragraph rewritten with measured + post-truncation numbers + cache-key implication + logging note; DESIGN_SYSTEM §2.12 new `PairBriefExportButton` entry beside `BriefExportButton`; README §What-it-doesn't-do "No PDF/markdown export" line removed (shipped); new memory `feedback_measure_token_volume.md` + MEMORY.md entry: rebuild prompt JSON and count chars before scaling any LLM batch >10 calls; treat ARCHITECTURE cost figures as stale-suspect. Session-43 cumulative LLM spend **~$56.40** (the $54 pre-truncation blowup on the 200-pair attempt + $2.40 post-fix completion).
**Session 44 ships:** Operator-reported heatmap bug fixed — drilling an aspect cell (e.g. a red/negative Thermals cell) opened the EvidenceDrawer showing each quote's *first* (arbitrary) aspect chip instead of the drilled aspect, so a negative cell drilled into positive-looking `performance`/`display` chips (contradiction). Root cause was display-only — the aggregate was always internally consistent. Fix lives in the shared drawer + card so all three drill surfaces (heatmap · Standalone aspect · Pair cell) are corrected at once: `EvidenceDrawer` threads its drilled `aspect` + `productId` into each `VerbatimCard` as `focusAspect`/`focusProductId`; the card renders the tag matched on `(aspect, product_id)` (load-bearing on comparison articles tagged for ≤5 products), omitting rather than mislabeling when absent; the drawer's intensity filter is now scoped to the drilled aspect; `_mention_to_view` sorts `aspect_tags` into canonical `Aspect`-enum order so the no-focus fallback (pooled `CitationPanel` / Showcase) is deterministic. `types.ts` `AspectTag` gains `product_id`. Plus a latent-risk fix: `/api/mentions` now filters tags to the current `(taxonomy_version, prompt_version)` — matching what the cell aggregates — so a future re-tagging pass can't surface a stale-version tag whose polarity disagrees with the cell colour (no live bug today; DB holds one version). Verified live in-app (Playwright): the four mentions from the bug report now render `thermals` chips (neg/neg/pos/neg) = the −0.50 cell. +1 backend canonical-order test, +1 backend version-filter test, +4 `VerbatimCard`, +2 `EvidenceDrawer`. No schema/migration, no LLM run, no aggregation change. Docs: DESIGN_SYSTEM §4.8 (card chip contract + drawer-is-aspect-scoped), ARCHITECTURE §13 re-tagging note, TESTING §7.
**Session 45 ships:** Daily ingestion — step 1 of a new three-tier cadence (daily collect / weekly analyze / quarterly brief) adopted after the operator surfaced that raw Reddit data is perishable (only ~1000 items page back in a sub's "new" feed, gone in days, no backfill) — so collection must run daily even though synthesis stays quarterly. Design + locked decisions in new `docs/DAILY_INGESTION_DESIGN.md` (operator approved: 3-cadence model · aggregate trend storage = option A per-week snapshot id, no migration · weekly aggregate = full corpus-to-date). New `scripts/daily_collect.py` (thin wrapper over `run_scrape`; no LLM; gap detection logs a loud WARNING if >36h since last successful run, perishable-data-in-gap unrecoverable; writes state only on success so a crash never advances the timestamp) + `pulse_check/scheduling/daily_state.py` (`DailyCollectorState` mirror of quarterly `state.py`: atomic schema-versioned JSON at `data/daily_collector_state.json`, `gap_hours`/`has_gap` helpers) + `Settings.daily_collector_state_path` (alias `DAILY_COLLECTOR_STATE_PATH`). +15 unit tests in `tests/unit/scheduling/test_daily_state.py` (read/write/round-trip/version-mismatch/atomic-no-temp + gap detection: never/healthy/missed-day/threshold/custom). No schema/migration, no LLM run, no frontend change. First live daily run is operator-gated (network/bandwidth; operator owns scheduling + timing vs the other project on this machine). Steps 2–4 (per-week aggregate snapshots · weekly-analyze entry point · trend read path) not yet built. Docs: ARCHITECTURE §14 retitled to the 3-cadence model + new §14.1 daily ingestion / §14.2 quarterly skeleton; README "Quarterly refresh procedure" → "Refresh cadence" with daily/weekly/quarterly breakdown + daily-collect command + non-goal line reconciled; TESTING §3 new `tests/unit/scheduling/` area.
**Session 46 ships:** Daily-ingestion steps 2–4 + scheduler registration, then a live smoke exposed that the whole Reddit/YouTube collection layer needed rebuilding. Reddit rebuilt on `.rss` (JSON API permanently 403-blocked for our IP; posts + comments + inheritance preserved, comment-deepening capped at 50 newest posts/run + ~1 s throttle to dodge 429s; new `reddit_rss` / `reddit_comments_rss` fetchers in scrapers-lib). YouTube whisper audio fallback enabled (yt-dlp + faster-whisper, CPU/no-GPU, title+description selection + pre-fetch dedup of already-transcribed videos). Weekly-analyze entry point (`scripts/weekly_analyze.py`, Qwen default, skips briefs) + per-week aggregate snapshot (`pulse_check/scheduling/weekly_snapshot.py`, option A no migration) + `GET /api/trend/{product_id}` backend read path. Daily + weekly Windows Task Scheduler tasks registered (daily collect 05:00 · weekly analyze 07:00 Sunday, GPU-staggered). 749 unit at close (later reconciled — true session-46 baseline was 745; the 749 was an arithmetic over-count) · scrapers-lib 63 reddit tests · mypy 146 · ruff clean · no schema · $0 billable LLM.
**Session 47 ships:** Trend UI shipped end-to-end (Wave 6 follow-on to step 4). New `/trend` route + 4th home card; Company→Product cascade (off the `brand` field); aspect-picker chips; **Week/Month/Year granularity toggle** with frontend rollup (`frontend/src/lib/trendRollup.ts`) — month/year buckets sum volume and **volume-weight** net sentiment (faithful to "every mention = 1.0"). Hand-rolled zero-dependency SVG `TrendChart` (sentiment line + per-point value labels + volume bars; density-graceful — labels/x-ticks thin out as buckets grow). `/api/trend` now **filters to weekly-pattern run_ids** via new `is_weekly_run_id()` in `weekly_snapshot.py` (behavior reversal from "all snapshots" — flagged; ARCH §14.1 updated) so the pilot `run_wave5_v1` + `smoke_test` ids don't pollute the trend axis. Fixed a white-screen crash (selected-aspect render race → derive active aspect at render time + empty-bucket guard). Verified live: materialized `run_2026_w24` (aggregation-only via `--skip-tagging`, no GPU) → chart renders a real point. Also this session: dropped `r/OmenByHP` from `run_wave5_v1.yaml` (restricted 403); confirmed the first daily 05:00 cron fired clean (425 new mentions, 0×429, comment-cap + inheritance working, state advanced); cleared `data/scheduler_state.db` after the config change; reconciled the session-46 unit over-count (749 → 745 → 748 with +3 backend this session). +12 frontend tests (8 `trendRollup` + 4 `TrendChart`) + 3 backend (1 trend run_id filter + 2 `is_weekly_run_id`). No schema/migration · $0 billable LLM.

**Active:** **3 exit forks remain unblocked** — operator brief-review walk *(now reads 55 v3 A1 briefs with summary paragraph + 200 pair briefs on the Pair page; the pair page additionally exposes a 3-route export overlay)* · reproducibility re-run · stakeholder prep packet *(`docs/STAKEHOLDER_PACKET.md` draft from session 40 awaits operator review)*. **748/748 unit tests · 4/4 integration tests · 51/51 frontend tests (vitest) · mypy clean (146 src) · ruff clean · frontend tsc + vite build green.** (Plus scrapers-lib: 63 reddit/reddit-rss tests green after the `.rss` rebuild.) Current frontend bundle: `index-BtspTJgn.js` (335.90 KB / 97.54 KB gzipped; session 47 — Trend page + TrendChart + rollup util). Migration head: `eb05da255474` (unchanged; no schema work session 46 — daily-ingestion steps 2–4 use option A, no migration). New runtime deps: `scrapers-lib[youtube-audio]` (yt-dlp + faster-whisper) for YouTube whisper transcription.
**Last reconciled:** 2026-06-11

**Waiting on operator action (cross-wave):**
- Operator visual confirm against live uvicorn (Wave 2 frontend smoke completed; ~5-min browser walk; screenshots at `C:/Users/AW-testing/AppData/Local/Temp/smoke_11_3_c/`)

**Doc index:** spec in [`PRD.md`](PRD.md) + [`ARCHITECTURE.md`](ARCHITECTURE.md). UI in [`DESIGN_SYSTEM.md`](DESIGN_SYSTEM.md). Eval methodology in [`TESTING.md`](TESTING.md). Bite narratives + audit checklists in [`SESSION_LOG.md`](SESSION_LOG.md).

---

## Wave 1 — Foundation ✅

**Goal:** Shared layer is working — scrape, attribute, store, cache; frontend/backend shells ready for aspect work.
**Exit:** Alembic schema created · 2-product smoke run succeeds · unified corpus with all 5 sources in SQLite · LLM cache populated · FastAPI + React shells running.

- [x] Package scaffold (`pyproject.toml`, `.gitignore`, pre-commit, pytest config, logging)
- [x] SQLAlchemy + Alembic (engine, session factory, declarative base, full schema)
- [x] Config system (Pydantic models, YAML loader, validation, example configs)
- [x] Scrapers-lib integration (wrappers for Reddit, BestBuy, Amazon, YouTube, articles)
- [x] Attribution (primary regex at fetch time + post-fetch secondary sweep)
- [x] LLM cache + clients (`call_with_cache` + Ollama + Anthropic wrappers)
- [x] Frontend/backend shells (Vite + React + TS + Tailwind + shadcn; FastAPI + CORS + routes)

---

## Wave 2 — Aspect 1 (A1-only cutpoint) ✅ substantively

**Goal:** Full A1 scorecard per product; aspect classifier passes gold-set threshold; end-to-end demo on 2 products.
**Exit:** Classifier ≥80% on gold set · `/product/:id` renders AspectRows × 11 + BriefPanel with drillable EvidenceDrawer · one product produces citation-validated A1 brief · re-run produces byte-identical outputs.

- [x] Aspect + polarity + intensity classifier (Haiku)
- [x] Content-type pre-classifier + `--exclude-content-types` gate
- [x] Aspect batch tagger with caching + within-response dedup
- [x] Reddit comment-inheritance (parent-post PRIMARY → comment SECONDARY)
- [x] Gold-set build + review CLIs (Sonnet-labeled)
- [x] A1 aggregator with PRIMARY/SECONDARY dual-bucket split
- [x] Haiku near-duplicate clusterer (cached)
- [x] Deterministic verbatim selector (intensity-ranked, cluster-deduped)
- [x] Sonnet A1 brief writer (four-quadrant routing, citation contract)
- [x] Citation validator (fabricated IDs, out-of-context, drift, empty claims)
- [x] `synthesize_a1` orchestrator + CLI; first live briefs (Alienware + ROG)
- [x] Backend API (`/products`, `/product/:id`, `/mentions?ids=`, `/brief/:id`)
- [x] Frontend atoms (Chip, DrillNumber, CiteChip, SourceMark, SourceDots, IntensityBar, Sparkline, VerbatimCard)
- [x] Frontend composites (EvidenceDrawer, CitationPanel, RunMetaStrip)
- [x] Frontend pages (About, Standalone, Compare placeholder; hash router; AspectColumn 3-section partition)
- [x] Frontend selectors + live API wiring (dropdowns, BriefPanel keyword routing)
- [x] Headless visual smoke (Playwright, 13/13 functional checks, all routes)
- [x] Aspect-tagging eval runner (`scripts/run_eval.py`; closure at **loose F1=0.78** / strict F1=0.60 on v2 gold via `compute_micro_f1_loose` ±1 intensity-bucket tolerance; THRESHOLD=0.80 kept and not formally met but operator-accepted as Wave 2 closure)
- [x] Home page: 3-card surface (Standalone / Head-to-head / Heatmap) + collapsible "Under the hood" plain-English pipeline explainer + sources accordion
- [x] Standalone page Company/Product picker (port from About picker; routes via `useNavigate`)
- [x] Head-to-head Pair page (`/pair`) — A1-based aspect scorecard with PickerColumns (no default selection), 3 CountTiles (Primary leads / Ties / Competitor leads) above a single combined PairTable sorted by combined `total_mentions` with leader-side accent ring on `PairCellChip`
- [x] Cross-product heatmap (`/compare`) — Company column with brand banners, Alienware-pinned + 2px accent edge, 3 multi-select popovers (Company / Screen size / Product) with cascading Company → Size → Product filtering, sortable headers on Company + 11 aspects with click-cycle asc/desc/clear, color-scale legend above the grid, row-hover dim affordance via `display:contents` group, screen-size regex `(?<!\d)(13|14|15|16|17|18)(?!\d)` recovers model suffixes (16x / 16s / Z13 / X16) and size-less products always pass
- [x] Brief schema v2 (`Claim.header`, prompt_version `a1_brief_v2` + `_strict`); 53 briefs regenerated under `run_wave5_v1`
- [x] Backend API surface for new pages: `/api/home` (run + pipeline stats), `/api/pair` (head-to-head), `/api/compare` (heatmap), `/api/sources` (operator-curated source list)
- [x] Public deployment via Tailscale Funnel (`/pulse-check` → uvicorn :8765, same-origin SPA from `frontend/dist`)
- [ ] Operator visual confirm against live uvicorn *(operator-side; broader review of all three pages pending)*
- [ ] BestBuy + Amazon retailer-reviews path *(deferred — 3 plumbing items)*
- [ ] Iterate aspect classifier prompt *(deferred to Wave 4 polish; aesthetics binary F1=0.62 is the one remaining aspect-level hole + parser drops on `unknown polarity 'mixed'` / duplicate-aspect responses cost ~8 TP per re-eval)*
- [ ] Full brief-generation integration test on fixture corpus *(deferred to Wave 4)*

---

## Wave 3 — Aspect 2 (A2 — comparative pairs)

**Parked, session 35.** Decision rationale: Reddit corpus is a comparison venue, not a confirmation venue — too few resolved-to-tracked-product deliberation threads to drive a reliable A2 artifact (0/50 in the v2 gold-set sample). All Wave 3 items below remain unchecked pending future revival; A2 schema tables retained empty in the DB. The head-to-head surface continues to ship as the A1-derived `/pair` page (A1 aggregate diffs only; no A2-driven brief layer).

**Goal:** Full A2 pair view for all 10 demo pairs; deliberation + outcome + reason classifiers pass thresholds.
**Exit:** Deliberation ≥85% · outcome ≥85% · reason ≥80% · intensity ≥80% · `/pair/:id` renders WinRateHeader + ReasonRows + BriefPanel for all 10 pairs · all briefs citation-validated.

- [ ] Deliberation thread classifier (Qwen): `is_deliberation`, `is_resolved`, `products_discussed`, `chosen_product_id`
- [ ] Reason tagger (Qwen): `reason_bucket` + polarity + intensity per comment in resolved threads
- [ ] Snapshot tests on classifier prompts
- [ ] Gold sets: `deliberation_v1` (built N=7 + operator-reviewed) · `reason_tagging_v1` (empty in v1 corpus → Wave 5 rebuild)
- [x] Deliberation classifier v2: `chosen_external_name` field for "tracked products lost to external winner" signal
- [ ] Eval extensions for `--task deliberation` and `--task reason_tagging`; iterate to thresholds
- [ ] A2 pair_win_rates aggregator (per-pair, resolved threads by `chosen_product_id`)
- [ ] A2 aggregates_pair_reason aggregator (per-pair-winner-reason with provenance)
- [ ] Considered-mention corpus enrichment (BestBuy/Amazon/YouTube/article via secondary attribution)
- [ ] Sonnet addressability classifier (messaging/software/hardware/pricing per reason bucket)
- [ ] Sonnet A2 verbatim selector + brief writer + citation validator
- [ ] API: `/pairs`, `/pair/:id`, `/threads?pair_id=`
- [ ] DESIGN_SYSTEM update for A2 atoms (WinRateHeader, ReasonRow, paired-table)
- [ ] Frontend: WinRateHeader, ReasonRow with addressability chip, two-column reason table
- [ ] Frontend page `/pair/:id` + pair selector + landing-page tabs

---

## Wave 4 — Eval + polish

**Goal:** All classifiers locked; UI polished against design system; edge cases handled.
**Exit:** Full eval suite green · DESIGN_SYSTEM tokens fully implemented · empty/loading/error states rendered · WCAG AA accessibility · tombstoning tested.

- [ ] Eval iteration on any classifier near but below threshold
- [ ] Gold-set refinement (add difficult edge cases from spot-check)
- [ ] Design-system full rollout (tokens, font loading, all shadcn components themed)
- [x] Empty states (About / Standalone / Compare already shipped; Pair idle-state explainer card added — "Pick a product on each side")
- [x] Loading skeletons — `frontend/src/components/ui/skeleton.tsx` across Compare / Standalone / Pair / EvidenceDrawer / CitationPanel
- [x] Error states (API failure, stale data, malformed response) — error cards with retry on all 4 pages
- [x] Accessibility — global `*:focus-visible` ring + `prefers-reduced-motion` rule already in `index.css`; Wave 4 polish added `aria-controls` on accordions, `Escape`-to-close on MultiSelectPopover, descriptive `aria-label` on HeatCell sentiment buttons + sort headers
- [ ] Performance (drawer pagination >500 mentions, aggregate query indexes)
- [x] Tombstoning — `scripts/verify_mention_links.py` + `pulse_check/verifiers/link_verifier.py` + reddit-domain throttle (`_is_reddit_host` + `threading.Lock` + 1.5s sleep gate) + aggregation filter + `MentionView` API + VerbatimCard chip; live HEAD batch on full 5061 corpus complete: 5028 ok / 33 retailer-403s / 0 tombstones
- [ ] Full brief-generation integration test on fixture corpus (carries the Wave 2 deferral)

---

## Wave 5 — Demo run + hardening

**Goal:** First full pilot run on the 59-product gaming-laptop competitive set; reproducibility proven; ready for stakeholder review.
**Exit:** Product-set config with all 59 products + regex · staged then full scrape+tag+aggregate+synthesize · all scorecards + Alienware pair briefs render cleanly · re-run produces byte-identical outputs · operator manually reviewed every brief.

- [x] Populate product-set config (59 products: aliases, attribution regex, collision-disambiguated patterns)
- [x] Wave 5 staged run config + staged pair plan (13-product subset, 36 Alienware-anchored pairs)
- [x] RSS-discovery module (`pulse_check/scraping/rss_discovery.py`) with YouTube + review-site feeds + `RSSWindow` + `RunConfig.rss_sources` + HTML auto-discovery fallback
- [x] Operator-supplied YouTube channel handles + review-site RSS feed URLs (`configs/wave5_rss_sources.yaml`)
- [x] Staged Wave 5 scrape (RSS-only, 13-product subset) + dry-run visibility CLI (`scripts/dry_run_rss_discovery.py`)
- [x] Notebookcheck Cloudflare bypass via scrapers-lib v1.4.0 (`warmed_curl_session` + curl_cffi Chrome120 TLS impersonation; pin in `pyproject.toml`)
- [x] Notebookcheck `catalog_discovery` module + CLI (`pulse_check/scraping/catalog_discovery.py` + `scripts/discover_notebookcheck.py`; POST search → URL-slug filter → per-product YAML for operator curation)
- [x] RSS title_keywords tightening (drop bare `review`/`best`; add `laptop review`/`best laptop`)
- [x] Full 59-product Notebookcheck catalog discovery run (`--min-published-date` flag + `filter_by_min_date` helper)
- [x] Operator curation pass (33 entries approved across 30 YAMLs in `data/discovered_urls/notebookcheck/*.yaml`; 29 quarantined to `_dropped_28a/`)
- [x] Orchestrator integration of approved discovered URLs (`RunConfig.discovered_urls_sources` field + `pulse_check/scraping/discovered_urls.py` module + `_enqueue_discovered_urls` helper + `scripts/scrape.py` passthrough)
- [ ] Manufacturer-dropdown POST extension — recon Notebookcheck's form for manufacturer-ID values, extend `search_notebookcheck` payload, retest quarantined mega-product queries
- [x] Live verification of `discovered_urls_sources` path at the full Wave 5 scrape (capability + output aha against actual corpus)
- [ ] (Optional) JS-rendered external-URL Playwright path — Notebookcheck spec/aggregation pages embed external review URLs via JavaScript
- [x] Article RSS-path strategy decision — superseded by catalog_discovery as primary; broad RSS feeds kept as opportunistic supplement
- [x] Full Wave 5 scrape on all 59 products (`reddit.enabled: true`, `rss.backfill_months: 6`, `discovered_urls_sources` in `run_wave5_v1.yaml`)
- [x] Full aspect tagging — 4,813 aspect_tag rows · 53/59 products tagged · 6 had zero qualifying mentions after DEAL filter
- [ ] Full deliberation/reason tagging overnight (Wave 3 A2 path)
- [x] Aspect-tagging gold-set rebuild on filtered corpus at target N=150 (carries Wave 2 formal ≥80% closure)
- [x] Deliberation + reason gold-set rebuild on expanded corpus (Wave 3 carry-over: v1 corpus had zero resolved-to-tracked-product threads)
- [x] Aggregation + synthesis for all A1 products (Stage A + Stage B under `run_wave5_v1` — 489 aggregate rows · 53 briefs · 44/50 Stage B briefs pristine · 6 with one numerical-drift each · 0 fabricated across all 50). A2 pair briefs deferred to Wave 3.
- [x] Brief regeneration at `a1_brief_v2` prompt (header field; 53 briefs re-written via `scripts/run_stage_b.py --skip-tagging --skip-aggregation --include-stage-a-briefs`)
- [x] Pair brief — single-contrast-paragraph A2 artifact (ARCH §6.3 pair brief layout; **200 distinct pair scope_ids** persisted as `scope_type=ASPECT_2_PAIR` rows on the existing `briefs` table covering 4 Alienware primaries × 50 non-Alienware competitors with aggregates; `pair_brief_v1` Sonnet; rendered on Pair page above the scorecard; `validate_pair_citations` clean across all 200; 240 brief rows total once cap-bump + post-truncation re-syntheses are counted, `_latest_pair_brief_id` returns the freshest per pair)
- [x] Comparative brief export — `PairBriefExportButton` + `frontend/src/lib/pairExportMarkdown.ts` mirror standalone `BriefExportButton` on the Pair page: 3 routes (PDF via `window.print`, self-contained HTML Blob, GFM Markdown). A4 sheet diverges where the data does — 3 stat tiles (primary leads / ties / competitor leads) · dual per-product 11-aspect chip rows · accent left-border italic contrast block · "Where {primary} leads" / "Where {competitor} leads" bullets (top 3 per side by `abs(delta) > 0.10`, each citing up to 2 mention_ids) · pooled citations footer. Wired right of `GlossaryButton` in the Pair page header conditional on both `pairState` and `briefState` being ready.
- [x] Pair-brief cost remediation — `VERBATIM_TEXT_CAP_CHARS = 800` truncation in `pair_brief_writer._verbs_block` + `brief_writer._truncate_verbatim`; per-pair Sonnet input dropped from ~165K → ~8.6K tokens (measured); per-pair cost ~$0.030 post-truncation vs ~$0.50 pre-truncation. Anthropic-client per-call usage logging (`anthropic call model=... in_tokens=... out_tokens=... est_cost=$x.xxxx`) added in `anthropic_client._call` with `_PRICING_PER_MTOKEN` table so future batches surface spend in-flight. ARCHITECTURE §6.3 cost paragraph rewritten with measured numbers + cache-key implication.
- [ ] Manual review of every brief + aggregate + verbatim attribution
- [ ] Bug-fix round from review findings
- [ ] Reproducibility test (re-run, verify cache hits + byte-identical outputs)
- [ ] Stakeholder review prep (screenshots, narrative, talking points)

---

## Wave 6 — Buffer

**Goal:** Slippage absorption; optional polish if Waves 1–5 land on time.

- [x] README refresh as operator runbook (orchestrator-first path · monthly refresh procedure · public deployment via Tailscale Funnel + uvicorn :8765 · granular controls subsection · fixed script names + npm)
- [x] PDF + HTML brief export (single-page A4 one-pager via `BriefExportButton`: 4 stat tiles + 11-aspect chip row + top 3 strengths/complaints + citations footer. PDF route = browser `window.print()` + `@media print` stylesheet. HTML route = Blob download with inlined styles. Wired on Standalone; component generic over `BriefView` so it transplants onto a future Pair brief.)
- [x] Glossary modal (`GlossaryDialog`, "What are all these numbers?") on Standalone / Pair / Compare — 6 entries × 5 sections (drop-rule: term must appear on screen AND be non-obvious to a cold visitor)
- [x] ASUS / ASUS ROG brand merge — single `ASUS` brand with 14 products in both `configs/product_set_gaming_laptops_2026.yaml` AND `data/pulse_check.db` `products` table (9 ROG rows UPDATE'd at the DB layer; YAML alone is insufficient since `/api/products` reads from DB at request time)
- [x] Home page CTAs converted from text-hyperlinks to purple-outlined buttons (`Open standalone` / `Open comparison` / `Open heatmap`) — flips to solid purple on hover
- [x] `EvidenceDrawer` verified-only filter hidden (corpus has zero `verified_purchase` signal until BestBuy/Amazon ingestion lands; aggregator + schema column retained)
- [x] Attribution regex refinement from scrape findings — MSI `a?` prefix on size digits across 11 products (22 line edits in `configs/product_set_gaming_laptops_2026.yaml`); cascaded +66 SECONDARY attributions / +85 aspect_tag rows / +11 aggregates / +9 briefs at `a1_brief_v2` under `run_wave5_v1`. AMD-variant naming policy: model-size optionally prefixed by `a` matches both Intel and AMD SKUs in a single pattern (ARCH §5 patch).
- [ ] Deferred non-blocking items from Open Questions
- [ ] (Optional v1.5) Markdown brief export, rolling-forward scheduler, tombstoning retention policy
- [~] Daily ingestion cadence (3-tier: daily collect / weekly analyze / quarterly brief; design in `docs/DAILY_INGESTION_DESIGN.md`). **Step 1 daily collect shipped (session 45)** — `scripts/daily_collect.py` + `pulse_check/scheduling/daily_state.py` + gap detection. **Steps 2–4 shipped (session 46)** — [x] step 2 weekly-analyze entry point (`scripts/weekly_analyze.py`, Qwen default, skips briefs) · [x] step 3 per-week aggregate snapshots (`pulse_check/scheduling/weekly_snapshot.py`, option A, no migration) · [x] step 4 trend read path (`GET /api/trend/{product_id}`; filtered to weekly run_ids session 47) + Trend UI shipped (`/trend`, session 47). **Scheduler artifacts + live registration (session 46)** — `scripts/daily_collect.{ps1,xml}` + `scripts/weekly_analyze.{ps1,xml}`; both tasks registered on the operator machine (daily collect 05:00 daily · weekly analyze 07:00 Sunday, GPU-staggered off the other project's 23:00 LLM run). Remaining: [x] first daily run verified (Thu Jun 11 05:00 — 425 new mentions, 0×429, comment-cap + inheritance clean, state advanced) · [ ] verify first weekly run (Sun Jun 14 07:00) from `data/weekly_analyze_log/`
- [x] Collection-layer fixes (session 46; surfaced building the daily collector — see `docs/DAILY_INGESTION_DESIGN.md` §"Collection-layer fixes" + ARCHITECTURE §5). Reddit JSON API 403-blocked → rebuilt on `.rss` (new `reddit_rss` / `reddit_comments_rss` fetchers in scrapers-lib; posts + comments + inheritance preserved; comment-deepening capped at 50 newest posts/run + ~1 s self-pace to dodge reddit's 429). YouTube `audio_fallback` enabled (yt-dlp + faster-whisper CPU transcription for caption-less videos) + title/description selection + pre-fetch dedup (skip already-transcribed videos). Verified live: reddit new=477 + inheritance new=842; whisper smoke 247 snippets. `pyproject.toml` now declares `scrapers-lib[youtube-audio]`.
- [x] Trend UI (follow-on to daily-ingestion step 4). Dedicated `/trend` route (not the Standalone page) + 4th home card. Company→Product cascade · aspect-picker chips · **Week/Month/Year granularity toggle** (frontend rollup, volume-weighted net sentiment) · hand-rolled zero-dependency SVG `TrendChart` (sentiment line + per-point value labels + volume bars; `recharts` rejected — no new dep). `/api/trend` filters to weekly run_ids (`is_weekly_run_id()`) so pilot/smoke snapshots stay off the axis. Renders real data once weekly snapshots accrue (one snapshot = one point, with an explicit single-point note); first genuine multi-point line builds from the Sun 6/14 weekly cron onward.
- [x] Drop `r/OmenByHP` from the run config (consistent 403 — restricted/quarantined sub; HP Omen chatter still flows via other subs).

---

## Parallel operator-side prerequisite

Operator-side setup running alongside code work; not blocking Wave 1 start.

- [x] Python 3.12 + `.venv`
- [x] Anthropic API key in `.env`
- [x] scrapers-lib editable install from sibling directory
- [x] Install Ollama + pull `qwen2.5:7b-q4_K_M` *(Wave 3 prereq — Wave 3 currently parked, but install in place for future revival)*
- [x] `playwright install chromium` *(for Tier 2/3 sources)*
- [x] YouTube channel handle list (7 channels) + review-site RSS feed URLs (9 sites) — see `configs/wave5_rss_sources.yaml`
