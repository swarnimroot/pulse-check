# Production build env. Used by `npm run build`.
# Empty VITE_API_URL → api.ts defaults to "./api" (relative to document).
# Keeps the build prefix-agnostic for any reverse-proxy mount path.
VITE_API_URL=
